id = Bebras-ID
title = ${common.title}
type = Antwoordtype
type-extra = Extra typeinformatie

filter {
    id = Filter op Bebras-ID
    title = ${common.filter.title}
}

# list questions
list {
    title = Vragen
    new = Nieuwe vraag
}

# upload file with several question details
upload {
    link = Vragen uploaden
    title = ${question.upload.link}
    caption = ${common.caption.upload}
    file = Bestand (werkblad). Enkel .xslx wordt ondersteund.

    languages = Taalvolgorde v/d titel- en antwoordkolommen
    languages-place-holder = "Vb: en, nl, fr"

    errors = "Er waren enkele problemen bij het opladen van de vraaggegevens. Zie hieronder"
    success = "Vragen met success opgeladen"
    error-no-file = "Kon bestand niet opladen"
    error-id = ${question.settings.error-id}
    error-lang-format = Ongeldig formaat
}

# upload question pages
pages {
    question-file = Opgavenbestand
    feedback-file = Feedbackbestand
    update = ${common.caption.update}
    upload = ${common.caption.upload}
    answer-code = Bepaal antwoordcode

    question-not-uploaded = Nog geen opgave geüploaded
    feedback-not-uploaded = Nog geen feedback-pagina geüploaded
    success = Bestanden met success geüpload
    error = Fout bij het uploaden van een bestand
}

# settings (new / edit)
settings {
    new = Nieuwe vraag
    id = ${question.id}
    type = ${question.type}
    type-extra = ${question.type-extra}
    language-place-holder = "Vb: en"
    error = Kon vraaggegevens niet bewaren
    success = Vraaggevens met succes bewaard
    error-id = "Er bestaat reeds een vraag met dit Bebras-ID"
}

# translations
translation {
    language = ${common.language}
    language-place-holder = "Vb: de"
    title = ${common.title}
    solution = ${common.solution}

    save = ${common.caption.save}
    add = "Vertaling toevoegen"
    remove = "Deze vertaling verwijderen"
    error-add = "Kon taal niet toevoegen aan vraag"
    error-remove = "Kan deze vertaling niet (meer) verwijderen"
}

