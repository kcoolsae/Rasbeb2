caption {
    download-scores = Download resultaten
    download-registrations = Download registraties
    download-pupils = Download leerlingen-spreadsheet
}

filename {
    scores = resultaten
    registrations = registraties
    pupils = leerlingen
}

# Table that lists upload errors
errors {
    row = Rij
    message = Foutmelding
}

# Upload pupils + table indicating errors
pupils {
    title = Fouten in de leerlingen-spreadsheet
    back = ${common.caption.back}
    link = Upload leerlingen-spreadsheet
    success = Leerlingen zijn toegevoegd
    error = Kon het bestand niet uploaden
    # errors when uploading pupils
    empty-class = Klas is verplicht
    unknown-class = Onbekende klas (voor deze school)
    empty-name = Naam is verplicht
    empty-gender = Gender is verplicht
    unknown-gender = "Genderaanduiding moet M, V of X zijn."
    duplicate-pupil = Er is in deze klas al een leerling met deze naam
}

# Download pupils - table headings
pupil {
    class = ${common.class}
    name = ${common.name}
    gender-short = "M/V/X"
    bebras-id = ${common.bebras-id}
    password = ${common.password}
}

# Download score - table headings
score {
    class = ${common.class}
    name = ${common.name}
    score = ${event.participations.score}
}
