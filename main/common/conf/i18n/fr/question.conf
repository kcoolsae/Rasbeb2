id = ID Bebras
title = ${common.title}
type = Type de réponse
type-extra = Info supplémentaire sur le type

filter {
    id = Filtrer par ID Bebras
    title = ${common.filter.title}
}

# list questions
list {
    title = Questions
    new = Nouvelle question
}

# upload file with several question details
upload {
    link = Charger les questions
    title = ${question.upload.link}
    caption = ${common.caption.upload}
    file = Fichier (tableur). Seul les .xslx sont pris en charge.

    languages = Ordre des langues des colonnes titre et réponse
    languages-place-holder = "Ex : en, nl, fr"

    errors = "Des problèmes sont survenus lors du chargement des détails de la question. Voir ci-dessous"
    success = "Questions chargées"
    error-no-file = "Impossible de charger le fichier"
    error-id = ${question.settings.error-id}
    error-lang-format = Format invalide

    invalid-answer-type = "Type de réponse invalide"
    blank-external-id = "L'ID Bebras ne peut pas être vide"
}

# upload question pages
pages {
    question-file = Fichier de la tâche
    feedback-file = Fichier du feedback
    update = ${common.caption.update}
    upload = ${common.caption.upload}
    answer-code = Déterminer le code de réponse

    question-not-uploaded = Page de tâche pas encore chargée
    feedback-not-uploaded = Page de feedback pas encore chargée
    success = Fichiers chargés
    error = Impossible de charger le fichier
}

# settings (new / edit)
settings {
    new = Nouvelle question
    id = ${question.id}
    type = ${question.type}
    type-extra = ${question.type-extra}
    language-place-holder = "Ex : fr"
    error = "Impossible d'enregistrer les données de la question"
    success = Données de la question enregistrées
    error-id = "Une question avec cet ID Bebras existe déjà"
}

# translations
translation {
    language = ${common.language}
    title = ${common.title}
    solution = ${common.solution}
    language-place-holder = "Ex : de"

    save = ${common.caption.save}
    add = "Ajouter une traduction"
    remove = "Supprimer cette traduction"
    error-add = "Impossible d'ajouter la langue spécifiée à la question"
    error-remove = "Plus possible de supprimer cette traduction"
}