id = Bebras ID
title = ${common.title}
type = Answer type
type-extra = Extra type info

filter {
    id = Filter by Bebras ID
    title = ${common.filter.title}
}

# list questions
list {
    title = Questions
    new = New question
}

# upload file with several question details
upload {
    link = Upload questions
    title = ${question.upload.link}
    caption = ${common.caption.upload}
    file = File (spreadsheet). Only .xslx supported.

    languages = Language order of title and answer columns
    languages-place-holder = "Ex: en, nl, fr"

    errors = "There were some problems while uploading question details. See below"
    success = "Questions uploaded successfully"
    error-no-file = "Could not upload file"
    error-id = ${question.settings.error-id}
    error-lang-format = Invalid format

    invalid-answer-type = "Invalid answer type"
    blank-external-id = "Bebras ID cannot be blank"
}

# upload question pages
pages {
    question-file = Task file
    feedback-file = Feedback file
    update = ${common.caption.update}
    upload = ${common.caption.upload}
    answer-code = Determine answer code

    question-not-uploaded = Task page not yet uploaded
    feedback-not-uploaded = Feedback page not yet uploaded
    success = Files uploaded succesfully
    error = Error while uploading file
}

# settings (new / edit)
settings {
    new = New question
    id = ${question.id}
    type = ${question.type}
    type-extra = ${question.type-extra}
    language-place-holder = "Ex: fr"
    error = Could not save question data
    success = Question data successfully saved
    error-id = "A question with this Bebras ID already exists"
}

# translations
translation {
    language = ${common.language}
    title = ${common.title}
    solution = ${common.solution}
    language-place-holder = "Ex: de"

    save = ${common.caption.save}
    add = "Add translation"
    remove = "Remove this translation"
    error-add = "Could not add specified language to question"
    error-remove = "Cannot (no longer) remove this translation"
}

