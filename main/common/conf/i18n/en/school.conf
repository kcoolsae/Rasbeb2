# Translatons related to schools and classes
class = ${common.class}

# Form on teacher's landing page
classes {
    title = Classes
    caption = Add classes
    # Form for new classes
    label = Classnames (separated by commas)
    submit = ${common.caption.submit}
    success-added = Classes added
    success-deleted = Class deleted
    success-updated = Class updated
    error-deleted = Failed to delete class
}

# Forms for adding a new pupil / updating pupil information
pupil {
    class = ${common.class}
    name = ${common.name}
    gender = ${common.gender}
    password = ${common.password}
}


## TODO clean up below
street = Street
zip = ZIP
town = Town
address = Address
address-warning = """Errors in name or address of the school should be
reported to an organisors. You can not correct them yourself."""
list-title = Schools
classes-list-title = "Pupils, by class"
teachers = Teachers
school = School
name = Name

filter.street = Filter by street
filter.zip = Filter by ZIP
filter.town = Filter by town
filter.school-name = Filter by schoolname

add = Add school
add-pupil = Add pupil
added = School successfully added
edit = Edit school information
register-teacher = Register a teacher for this school
edited = The school information was successfully changed
invalid-remove ="This school can't be deleted"
valid-remove = The school is successfully deleted
new-pupil {
    failed = "Could not add pupil"
    success = "New pupil added"
}
edit-pupil {
    failed = "Failed to edit the pupil. Check if all fields are filled in correctly."
    success = "Pupil edited"
}

account.status = Status
account.active = Active
account.inactive = Inactive

mimic = Mimic login
deactivate = Desactivate